import { ChangeImagesDDirective } from './change-images-d.directive';

describe('ChangeImagesDDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeImagesDDirective();
    expect(directive).toBeTruthy();
  });
});
